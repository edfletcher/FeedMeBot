{
  "name": "outage-bot",
  "version": "0.1.0",
  "description": "Service outage monitoring bot for IRC",
  "main": "index.js",
  "engines": {
    "node": ">=16.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "semistandard --fix --verbose"
  },
  "author": "Ed Fletcher",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.6",
    "date-fns": "^2.27.0",
    "inquirer": "^8.2.0",
    "irc-framework": "^4.12.1",
    "rss-parser": "^3.12.0"
  },
  "devDependencies": {
    "semistandard": "^16.0.1"
  },
  "semistandard": {
    "ignore": [
      "test/*"
    ]
  }
}
